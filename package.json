{
  "scripts": {
    "pagefind": "npx pagefind --site public --glob **/posts/*.html",
    "build": "hugo"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "prettier-plugin-go-template": "^0.0.15"
  }
}
